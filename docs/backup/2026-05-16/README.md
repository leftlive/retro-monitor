# 2026-05-16 Cleanup Archive

This archive keeps superseded files that should no longer be treated as active project entrypoints.

## Archived Items

- `prototype-status.md`: Python macOS validation snapshot from the earlier prototype era. Current macOS service docs live in `docs/project-progress-zh.md` and `docs/macos-agent-service.md`.
- `vendor-demo-code/`: upstream/demo ESP32 and VFD example code used as reference material while building the ESPHome VFD driver. The active VFD implementation now lives in `esphome/vfd_016st106ink_ha_monitor.yaml` and `esphome/includes/vfd_016st106ink/vfd_016st106ink.h`.
- `windows-agent-generated/`: Windows publish/package/NuGet generated output from a prior packaging run. Source and reusable scripts remain under `windows-agent/`.
